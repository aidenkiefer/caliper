{
  "name": "quant-ml-trading",
  "version": "0.1.0",
  "private": true,
  "description": "Quantitative ML Trading Platform - Monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=apps/dashboard",
    "build": "npm run build --workspace=apps/dashboard",
    "lint": "npm run lint --workspace=apps/dashboard"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
